[package]
name = "netwatch"
version = "0.5.0"
edition = "2021"
description = "OpenBSD stub for netwatch - provides API compatibility but no functionality"
license = "MIT OR Apache-2.0"

[dependencies]
futures = "0.3"
tokio = { version = "1", features = ["sync"] }
log = "0.4"
